{
  "name": "slack-webhook-nodejs",
  "version": "1.1.0",
  "description": "@slack/webhook Lambda Layer",
  "repository": "https://github.com/elastic-infra/lambda-layer-slack-webhook-nodejs",
  "license": "MIT",
  "scripts": {
    "version": "sed -i \"s/SemanticVersion: .*/SemanticVersion: $npm_package_version/\" ../template.yaml && git add ../template.yaml package.json && git commit -m \"bump up v$npm_package_version\""
  },
  "dependencies": {
    "@slack/webhook": "^6.1.0"
  }
}
